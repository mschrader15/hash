[package]
name = "server"
version = "0.0.0"
edition = "2021"

[dependencies]
hash_engine = { path = "../.." }

anyhow = "1.0.51"
tokio = { version = "1.5.0", features = ["macros", "rt-multi-thread", "sync", "process", "io-util", "net", "rt", "fs"] }
tracing = "0.1.29"

[[bin]]
name = "hash_engine"
path = "src/main.rs"
